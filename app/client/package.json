{
    "name": "client",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "bunx --bun tsc -b && bunx --bun tsc -p tsconfig.sw.json --noEmit && vite build && vite build --ssr src/entry-server.tsx && bun build src/sw.ts --outfile dist/client/sw.js --target browser && bun build src/server.ts --outdir dist/server --target bun",
        "dev": "vite",
        "preview": "bun run dist/server/server.js",
        "test": "vitest run",
        "typecheck": "bunx --bun tsc --noEmit --skipLibCheck false && bunx --bun tsc -p tsconfig.sw.json --noEmit"
    },
    "devDependencies": {
        "@testing-library/jest-dom": "^6.9.1",
        "@testing-library/react": "^16.3.2",
        "@types/jest": "^30.0.0",
        "@types/react": "^19.2",
        "@types/react-dom": "^19.2",
        "@typescript/lib-dom": "npm:@types/web",
        "@vitejs/plugin-react": "^6.0",
        "@vitest/coverage-v8": "^4.1.8",
        "hono": "^4.12.28",
        "jsdom": "^29.1.1",
        "react": "^19.2",
        "react-dom": "^19.2",
        "react-router-dom": "^7.5",
        "typescript": "^6.0",
        "vite": "^8.0",
        "vitest": "^4.1.8"
    }
}
